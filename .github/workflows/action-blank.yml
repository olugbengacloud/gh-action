name: Workflowss
on: push

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting and goodbye
        run: |
            echo "Hello world"
            echo "Good day, bye"
